# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# CurseForge AUTHORS API token — uploads, game-version lists.
# https://authors-old.curseforge.com/account/api-tokens  (UUID format)
CURSEFORGE_API_KEY=
