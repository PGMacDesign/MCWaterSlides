package com.pgmacdesign.mcwaterslides.slide;

/**
 * Marker for blocks riders slide through (channels, tubes). Connection logic, current
 * fields, and the ride ticker all key on this rather than concrete block classes, so
 * channels and tubes interconnect and ride identically.
 */
public interface SlideSurface {
}
